# 2026-07-27 — P-4 lands; OP-18 resolved modulo promotion

**Formalizer** submitted P-4 (`proofs/p004_k_invariance_global_existence.md`,
PR #1): K-invariance of the coupled layers under a Nagumo tangency condition
U(F, δ) ∈ T_K(F) on closed convex K, a continuation criterion, and global
existence under a two-layer coercive balance estimate closed by Grönwall.

**Writer review** (full pass, recorded on PR #1):

- (a) product tangent-cone identity T_{X×K} = X × T_K for convex K plus
  Nagumo/Brezis–Martin invariance for locally Lipschitz fields — sound;
  the closed-convex scope is honestly limited in R2.
- (b) inherits P-2's ball-uniform locally-Lipschitz convention (L_R valid
  whenever all norms ≤ R) exactly, so the bound M on G is legitimate; the
  restart-at-the-limit continuation argument is standard and correct. One
  duplicated line removed (consistency fix, allowed without asking).
- (c) the summed Grönwall closure with A = a_X + a_𝓖 + b_X + b_𝓖 checks
  term by term; the result is explicitly conditional on the existence of
  the balance pair (B_X, B_𝓖) — honest sufficient-condition form.

Merged to main (0fd2fa1). Book and ledger updated by the PR itself:
P-2's R1/R3 now point at P-4; P-1's evolving-class placement note points at
P-4's two-layer balance criterion; OP-18 marked **RESOLVED modulo maintainer
promotion** (P-2 local + P-4 invariance/global).

**Gate note.** The promotion of OP-18's status (and P-4 alongside it) is the
maintainer's call per AI_PROTOCOL; nothing here presumes it.

**What OP-18's resolution buys.** The evolving class is now a well-posed
object under explicit hypotheses: admissibility is enforced by tangency of
the update law at ∂K, and global-in-time evolution is available whenever a
two-layer balance closes. The live front narrows to **OP-17** (canonical
split) and **OP-14** (instability route to persistent complexity), with
OP-16 still gating any external novelty claim.

Repo tally after eight steps: four propositions with complete proofs
(P-1, P-2, P-3, P-4), one conjecture UNDER ATTACK (C-001 v2), OP ledger
with OP-18 resolved-pending-promotion.
