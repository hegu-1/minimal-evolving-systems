# 2026-07-28 — Fifth intake: the cognitive-state anchor definition

**Intake (relayed by the maintainer).** Proposed starting definition:
"cognitive state C(t) = the minimal internal representation sufficient to
predict the agent's future cognitive evolution and decisions without access
to its full history," with the accompanying observation that the motivating
questions all reduce to *can history be compressed into state?*

**Writer disposition** (`mappings/cognition.md`, rewritten):

1. **Placement.** The definition is the standard state concept — predictively
   sufficient statistic — with hard lineage: Kalman minimal realization,
   Crutchfield–Shalizi causal states / ε-machine (which answers "which
   histories compress and what is retained" exactly), predictive state
   representations, Mori–Zwanzig (the exact cost of under-compression).
   OP-16 applies at full strength: no novelty at this layer.
2. **Falsifiability sharpened.** As received it cannot fail (C = full
   history is always sufficient). Restated to quantify over *bounded,
   minimal, stable* C; divergent-statistical-complexity processes make the
   criterion measurable rather than rhetorical.
3. **Inside 𝓜.** Sufficiency is a theorem consequence, not a definition:
   P-2 + P-4 (OP-18) make (δ, F) a complete state under explicit
   hypotheses. The unplowed face is the law layer (sufficient statistic for
   F_t under non-trivial U — candidate delta (ii) of OP-16). OP-17
   translates: the content/rule split of C_t is presentation-relative.
4. **Consistency fix.** `mappings/cognition.md` still carried v1 symbols
   (X, Φ, K, B); propagated to v2 per the supersession chain (boundary
   rule: allowed without asking).

**Attack recorded** (`proofs/conjectures.md`, C-001 v2): the
*predictive-minimality attack* — clause (ii) does not exclude idle U on an
inflated history-space representation, so C-001 v2 is again satisfiable for
everything; candidate repair is a third non-degeneracy clause (predictive
minimality), which also turns divergent-statistical-complexity processes
into a live falsification family. C-001 stays UNDER ATTACK; the statement
needs clause (iii) before mapping proofs count as evidence.

**Net state.** Anchor definition housed as DISCUSSION in the cognition
mapping; ROADMAP item 4 now has a concrete candidate instantiation
(Bayesian filtering with model revision, exploiting the recursion: belief
state = content-layer sufficient statistic; the proof asks the same of the
law layer). Live front unchanged: OP-17, OP-14; OP-18 still awaiting
maintainer promotion.
